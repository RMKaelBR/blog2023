<h1>Posts</h1>
<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post.id) %>
      <%#<<a href="/posts/<%= post.id%><%#">>
        <%#<<%= post.title %>
      <%#<%= link_to post.title, post%>
      <%#<</a%>
    </li>
  <% end %>
</ul>
